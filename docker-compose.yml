services:
  metro-frontend:
    build: ./metro
    ports:
      - "4200:80"
    restart: unless-stopped

# metro-sim (Scala/Pekko) is not containerized due to JVM image size.
# Run manually: cd metro-sim && sbt run
# It serves WebSocket directly on ws://localhost:8081/ws — no relay needed.
